fix: SSG connection form and guide updated with IP direct-input method

- Required: login ID/PW + API auth key (UUID)
- Optional: SHOP ID, department store flag (Y/blank)
- Added purple IP registration block for both ops and test server IPs
- Guide: 3-step process with single direct-input IP registration
- Removed: Partner ID / API Key / Secret Key fields
