fix: halfclub and gsshop connection form and guide updated

halfclub: login ID/PW + partner code (SA...) + API auth key (UUID) required
         수수료(주문)% optional, FTP fields removed
gsshop: login ID/PW + shipment lead time (days) required, SHOP ID optional
        Vendor ID / API Key / Secret Key fields removed
Both guides updated with exact SCM menu navigation steps
