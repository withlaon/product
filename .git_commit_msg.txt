fix: separate saveCredentialsOnly and startOAuth for OAuth malls

- saveCredentialsOnly(): saves form data only, no OAuth popup (Settings Save button)
- startOAuth(): saves + opens OAuth popup (OAuth Re-auth button)
- Both buttons now work independently - clicking Settings Save no longer triggers OAuth
- Fixes bug where Settings Save was triggering OAuth flow and causing expired code errors
