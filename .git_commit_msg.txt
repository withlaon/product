fix: 주문관리 매핑하기 후 바코드가 비어있는 채로 남는 문제 수정

- (원인1) 자동매핑이 정형 규칙(matchOption)으로 옵션 텍스트를 못 찾으면 그 즉시
  포기하고 바코드를 비워둔 채 넘어갔음(몰마다 옵션 표기 방식이 달라 실패가 잦았음)
  → lib/orders.ts의 공용 퍼지 매칭(autoMatchBarcode, 색상/유사도 기반)을 2차 시도로
  추가해 정형 규칙이 실패해도 최대한 바코드를 찾아내도록 보강
- (원인2) 매핑 목록에서 "연결됨(초록색)" 표시가 product_id만 있으면 뜨고 barcode 유무는
  보지 않아, 실제로는 바코드가 비어있는데도 매핑이 끝난 것처럼 보였음(화면 캡처의
  "- 603_블랙 양산" 등) → product_id + barcode 모두 있어야 "연결됨"으로 표시하고,
  barcode만 없는 경우 "바코드 미확인" 주황색 경고로 명확히 구분. "미연결만 보기" 필터도
  동일 기준으로 수정해 반쪽 매핑 항목이 필터에서 숨겨지지 않도록 함
- 매핑 저장 직전에 한 번 더 자동 보완 시도를 수행하고, 저장 후에도 바코드를 못 찾은
  상품이 있으면 상품명 목록으로 명확히 알림 — 조용히 누락되는 일이 없도록 함
