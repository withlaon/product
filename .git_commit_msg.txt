fix: 출고내역 바코드 미표시 - 매핑 저장 시 다른 상품 매핑이 통째로 삭제되던 치명적 버그 수정

- (핵심 원인) 주문관리탭 "매핑하기" 저장 시 그 자리에서 편집 중인 주문(오늘/선택분)의
  매핑 키만 들어있는 draftMappings를 그대로 saveMappings()에 넘겨 전체 매핑 저장소
  (pm_product_mapping_v1)를 덮어써, 다른 날짜에 이미 매핑해둔 상품들의 바코드가
  매핑을 저장할 때마다 통째로 사라지고 있었음 → 항상 최신 전체 매핑에 병합 저장하도록 수정
- 주문서 등록탭: 마켓플러스 자동 매핑 저장 시에도 abbreviation/loca만 있는 새 객체로
  덮어써 기존 barcode가 지워지던 문제를 기존 매핑에 병합하는 방식으로 수정
- 주문서 등록탭에 상품 캐시 기반 바코드 자동매칭(lib/orders.ts의 공용 autoMatchBarcode)을
  추가해 상품명(관리용)/상품명+옵션으로 바코드를 자동 추정해 item.sku에 채우고 매핑에도
  저장 → 이후 출고내역 등 모든 탭에서 바로 바코드가 보이도록 개선
- 직접등록 폼: 자동 조회된 바코드가 sku가 아닌 extra_data에만 저장되어 유실되던 버그 수정
- 출고내역 탭의 자동매칭 로직을 lib/orders.ts로 공용화하여 화면 간 로직 불일치 방지
